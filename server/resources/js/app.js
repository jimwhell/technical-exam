import "./bootstrap";
import "./employees/employees";

/*
 * Alpine.js is included here as part of the Laravel Breeze scaffolding.
 * It is not actively used in this project — all interactivity is handled
 * via vanilla JavaScript.
 */
import Alpine from "alpinejs";

window.Alpine = Alpine;

Alpine.start();
